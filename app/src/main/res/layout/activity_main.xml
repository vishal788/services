<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".MainActivity">

    <TextView
        android:id="@+id/text1"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Start service"
        android:textSize="20dp"
        android:layout_centerInParent="true"
        />

    //android services is a component that is use to performd that is performed such a playing music handle network
    transaction interacting content etc. services run in the background indefinately
    even if application is destroyd.

</RelativeLayout>